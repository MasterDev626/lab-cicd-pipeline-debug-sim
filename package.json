{
  "name": "cicd-pipeline-lab",
  "version": "0.1.0",
  "description": "CI/CD Pipeline Debug & Release Simulation Lab",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --silent",
    "lint": "node -e \"require('fs').readFileSync('src/app.js'); require('fs').readFileSync('src/index.js'); process.exit(0)\"",
    "build": "mkdir -p dist && cp -r src dist/ && cp package*.json dist/"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "cicd",
    "pipeline",
    "devops",
    "testing",
    "docker"
  ],
  "author": "MasterDev626",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
